# The default (coming from util-linux) fstrim.service in DozenOS fails to
# perform any trimming because it stops evaluating mount points after
# reading /etc/fstab, which contains only overlay filesystems that
# fstrim inherently ignores.
# More info: https://dozenos.dev/T9133
[Service]
ExecStart=
ExecStart=/sbin/fstrim --listed-in /proc/self/mountinfo --verbose --quiet-unsupported