# Generated by ${dozenos_conf_scripts_dir}/vpn_ipsec.py

charon {
    syslog {
        # prefix for each log message
        identifier = charon
        # use default settings to log to the LOG_DAEMON facility
        daemon {
{% if log.level is dozenos_defined %}
            # Disabled - logging handled exclusively by charon-systemd.conf
            default = -1
{% else %}
            default = 1
{% endif %}
            ike_name = yes
        }
    }
}
