### autogenerated by system_proxy.py ###
{% if url is dozenos_defined and port is dozenos_defined %}
{# remove http:// prefix so we can inject a username/password if present #}
export http_proxy=http://{{ username ~ ':' ~ password ~ '@' if username is dozenos_defined and password is dozenos_defined }}{{ url | replace('http://', '') }}:{{ port }}
export https_proxy=$http_proxy
export ftp_proxy=$http_proxy
{% endif %}
