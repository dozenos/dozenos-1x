# Autogenerated by DozenOS
# Do not edit this file, all your changes will be lost
# on next commit or reboot

# Global definitions configuration block
global_defs {
    dynamic_interfaces
{% if vrrp.snmp.trap is dozenos_defined %}
    enable_traps
{% endif %}
    script_user root
{% if vrrp.global_parameters.startup_delay is dozenos_defined %}
    vrrp_startup_delay {{ vrrp.global_parameters.startup_delay }}
{% endif %}
{% if vrrp.global_parameters.garp is dozenos_defined %}
{%     if vrrp.global_parameters.garp.interval is dozenos_defined %}
    vrrp_garp_interval {{ vrrp.global_parameters.garp.interval }}
{%     endif %}
{%     if vrrp.global_parameters.garp.master_delay is dozenos_defined %}
    vrrp_garp_master_delay {{ vrrp.global_parameters.garp.master_delay }}
{%     endif %}
{%     if vrrp.global_parameters.garp.master_refresh is dozenos_defined %}
    vrrp_garp_master_refresh {{ vrrp.global_parameters.garp.master_refresh }}
{%     endif %}
{%     if vrrp.global_parameters.garp.master_refresh_repeat is dozenos_defined %}
    vrrp_garp_master_refresh_repeat {{ vrrp.global_parameters.garp.master_refresh_repeat }}
{%     endif %}
{%     if vrrp.global_parameters.garp.master_repeat is dozenos_defined %}
    vrrp_garp_master_repeat {{ vrrp.global_parameters.garp.master_repeat }}
{%     endif %}
{% endif %}
{% if vrrp.global_parameters.version is dozenos_defined %}
    vrrp_version {{ vrrp.global_parameters.version }}
{% endif %}
    notify_fifo /run/keepalived/keepalived_notify_fifo
    notify_fifo_script /usr/libexec/dozenos/system/keepalived-fifo.py
}

{# Sync group has own health-check scripts T6020 #}
{% if vrrp.sync_group is dozenos_defined %}
{%     for name, sync_group_config in vrrp.sync_group.items() if sync_group_config.disable is not dozenos_defined %}
{%         if sync_group_config.health_check is dozenos_defined %}
vrrp_script healthcheck_sg_{{ name }} {
{%             if sync_group_config.health_check.script is dozenos_defined %}
    script "{{ sync_group_config.health_check.script }}"
{%             elif sync_group_config.health_check.ping is dozenos_defined %}
    script "/usr/bin/ping -c1 {{ sync_group_config.health_check.ping }}"
{%             endif %}
    interval {{ sync_group_config.health_check.interval }}
{%             if sync_group_config.health_check.timeout is dozenos_defined %}
    timeout {{ sync_group_config.health_check.timeout }}
{%             endif %}
    fall {{ sync_group_config.health_check.failure_count }}
    rise 1
}
{%         endif %}
{%     endfor %}
{% endif %}

{% if vrrp.group is dozenos_defined %}
{%     for name, group_config in vrrp.group.items() if group_config.disable is not dozenos_defined %}
{%         if group_config.health_check is dozenos_defined %}
vrrp_script healthcheck_{{ name }} {
{%             if group_config.health_check.script is dozenos_defined %}
    script "{{ group_config.health_check.script }}"
{%             elif group_config.health_check.ping is dozenos_defined %}
    script "/usr/bin/ping -c1 {{ group_config.health_check.ping }}"
{%             endif %}
    interval {{ group_config.health_check.interval }}
{%             if group_config.health_check.timeout is dozenos_defined %}
    timeout {{ group_config.health_check.timeout }}
{%             endif %}
    fall {{ group_config.health_check.failure_count }}
    rise 1
}
{%         endif %}
vrrp_instance {{ name }} {
{%         if group_config.description is dozenos_defined %}
    # {{ group_config.description }}
{%         endif %}
    state BACKUP
    interface {{ group_config.interface }}
    virtual_router_id {{ group_config.vrid }}
    priority {{ group_config.priority }}
    advert_int {{ group_config.advertise_interval }}
{%         if group_config.garp is dozenos_defined %}
{%             if group_config.garp.interval is dozenos_defined %}
    garp_interval {{ group_config.garp.interval }}
{%             endif %}
{%             if group_config.garp.master_delay is dozenos_defined %}
    garp_master_delay {{ group_config.garp.master_delay }}
{%             endif %}
{%             if group_config.garp.master_repeat is dozenos_defined %}
    garp_master_repeat {{ group_config.garp.master_repeat }}
{%             endif %}
{%             if group_config.garp.master_refresh is dozenos_defined %}
    garp_master_refresh {{ group_config.garp.master_refresh }}
{%             endif %}
{%             if group_config.garp.master_refresh_repeat is dozenos_defined %}
    garp_master_refresh_repeat {{ group_config.garp.master_refresh_repeat }}
{%             endif %}
{%         endif %}
{%         if group_config.track.exclude_vrrp_interface is dozenos_defined %}
    dont_track_primary
{%         endif %}
{%         if group_config.no_preempt is not dozenos_defined and group_config.preempt_delay is dozenos_defined %}
    preempt_delay {{ group_config.preempt_delay }}
{%         elif group_config.no_preempt is dozenos_defined %}
    nopreempt
{%         endif %}
{%         if group_config.peer_address is dozenos_defined %}
    unicast_peer {
{%             for peer_address in group_config.peer_address %}
        {{ peer_address }}
{%             endfor %}
    }
{%         endif %}
{%         if group_config.hello_source_address is dozenos_defined %}
{%             if group_config.peer_address is dozenos_defined %}
    unicast_src_ip {{ group_config.hello_source_address }}
{%             else %}
    mcast_src_ip {{ group_config.hello_source_address }}
{%             endif %}
{%         endif %}
{%         if group_config.rfc3768_compatibility is dozenos_defined and group_config.peer_address is dozenos_defined %}
    use_vmac {{ group_config.interface }}v{{ group_config.vrid }}v{{ '4' if group_config['address'] | first | is_ipv4 else '6' }}
    vmac_xmit_base
{%         elif group_config.rfc3768_compatibility is dozenos_defined %}
    use_vmac {{ group_config.interface }}v{{ group_config.vrid }}v{{ '4' if group_config['address'] | first | is_ipv4 else '6' }}
{%         endif %}
{%         if group_config.authentication is dozenos_defined %}
    authentication {
        auth_pass "{{ group_config.authentication.password }}"
{%             if group_config.authentication.type is dozenos_defined('plaintext-password') %}
        auth_type PASS
{%             else %}
        auth_type {{ group_config.authentication.type | upper }}
{%             endif %}
    }
{%         endif %}
{%         if group_config.address is dozenos_defined %}
    virtual_ipaddress {
{%             for addr, addr_config in group_config.address.items() %}
        {{ addr }}{{ ' dev ' + addr_config.interface if addr_config.interface is dozenos_defined }}
{%             endfor %}
    }
{%         endif %}
{%         if group_config.excluded_address is dozenos_defined %}
    virtual_ipaddress_excluded {
{%             for addr, addr_config in group_config.excluded_address.items() %}
        {{ addr }}{{ ' dev ' + addr_config.interface if addr_config.interface is dozenos_defined }}
{%             endfor %}
    }
{%         endif %}
{%         if group_config.track.interface is dozenos_defined %}
    track_interface {
{%             for interface in group_config.track.interface %}
        {{ interface }}
{%             endfor %}
    }
{%         endif %}
{# Sync group member can't use own health check script #}
{%         if group_config.health_check is dozenos_defined and group_config._is_sync_group_member is not dozenos_defined %}
    track_script {
        healthcheck_{{ name }}
    }
{%         endif %}
}
{%     endfor %}
{% endif %}

{% if vrrp.sync_group is dozenos_defined %}
{%     for name, sync_group_config in vrrp.sync_group.items() if sync_group_config.disable is not dozenos_defined %}
vrrp_sync_group {{ name }} {
    group {
{%         if sync_group_config.member is dozenos_defined %}
{%             for member in sync_group_config.member %}
        {{ member }}
{%             endfor %}
{%         endif %}
    }

{%         if sync_group_config.health_check is dozenos_defined %}
    track_script {
        healthcheck_sg_{{ name }}
    }
{%         endif %}

{%         if conntrack_sync_group is dozenos_defined(name) %}
{%             set dozenos_helper = "/usr/libexec/dozenos/dozenos-vrrp-conntracksync.sh" %}
    notify_master "{{ dozenos_helper }} master {{ name }}"
    notify_backup "{{ dozenos_helper }} backup {{ name }}"
    notify_fault "{{ dozenos_helper }} fault {{ name }}"
{%         endif %}
}
{%     endfor %}
{% endif %}

{% if virtual_server is dozenos_defined %}
# Virtual-server configuration
{%     for vserver, vserver_config in virtual_server.items() %}
# Vserver {{ vserver }}
{%         if vserver_config.port is dozenos_defined %}
virtual_server {{ vserver_config.address }} {{ vserver_config.port }} {
{%         else %}
virtual_server fwmark {{ vserver_config.fwmark }} {
{%         endif %}
    delay_loop {{ vserver_config.delay_loop }}
{%         if vserver_config.algorithm is dozenos_defined('round-robin') %}
    lb_algo rr
{%         elif vserver_config.algorithm is dozenos_defined('weighted-round-robin') %}
    lb_algo wrr
{%         elif vserver_config.algorithm is dozenos_defined('least-connection') %}
    lb_algo lc
{%         elif vserver_config.algorithm is dozenos_defined('weighted-least-connection') %}
    lb_algo wlc
{%         elif vserver_config.algorithm is dozenos_defined('source-hashing') %}
    lb_algo sh
{%         elif vserver_config.algorithm is dozenos_defined('destination-hashing') %}
    lb_algo dh
{%         elif vserver_config.algorithm is dozenos_defined('locality-based-least-connection') %}
    lb_algo lblc
{%         endif %}
{%         if vserver_config.forward_method is dozenos_defined('nat') %}
    lb_kind NAT
{%         elif vserver_config.forward_method is dozenos_defined('direct') %}
    lb_kind DR
{%         elif vserver_config.forward_method is dozenos_defined('tunnel') %}
    lb_kind TUN
{%         endif %}
{%         if vserver_config.persistence_timeout | int > 0 %}
    persistence_timeout {{ vserver_config.persistence_timeout }}
{%         endif %}
    protocol {{ vserver_config.protocol | upper }}
{%         if vserver_config.real_server is dozenos_defined %}
{%             for rserver, rserver_config in vserver_config.real_server.items() %}
    real_server {{ rserver }} {{ rserver_config.port }} {
        weight 1
{%                 if rserver_config.health_check.script is dozenos_defined %}
        MISC_CHECK {
            misc_path {{ rserver_config.health_check.script }}
{%                 else %}
        {{ vserver_config.protocol | upper }}_CHECK {
{%                     if rserver_config.connection_timeout is dozenos_defined %}
            connect_timeout {{ rserver_config.connection_timeout }}
{%                     endif %}
{%                 endif %}
        }
    }
{%             endfor %}
{%         endif %}
}
{%     endfor %}
{% endif %}
